# Ignore everything by default (runtime state)
*

# Keep the legacy directory safe for unmigrated machines while retaining only
# shared assets that are still consumed by Pi or other agent tooling.
!.gitignore
!skills
!references/
