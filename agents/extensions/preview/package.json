{
  "name": "pi-extension-preview",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Headless browser preview tools for pi: launch/point at a web app, screenshot it back into the model's context, and inspect console/network/DOM.",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
