{{- $config := lstat (joinPath .chezmoi.destDir ".config") -}}
{{- if and $config (eq $config.type "symlink") -}}
{{ fail "refusing chezmoi operation: destination .config is still a symlink; use the migration command before applying this source" }}
{{- end -}}
{{- if ne .chezmoi.os "darwin" }}
.config/karabiner/
{{- end }}
