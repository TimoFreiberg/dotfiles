set $mod Mod4

# Exit window
bindsym $mod+F4 exec ~/bin/powerwidget.sh

# Lock Screen
bindsym $mod+shift+x exec ~/bin/lock

# Volume up and down
bindsym XF86AudioRaiseVolume exec pactl set-sink-mute 0 false ; exec amixer -D pulse sset Master 5%+
bindsym XF86AudioLowerVolume exec pactl set-sink-mute 0 false ; exec amixer -D pulse sset Master 5%-
bindsym XF86AudioMute exec amixer -D pulse sset Master toggle

# screen brightness up and down
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -dec 10
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -inc 10

# toggle touchpad
bindsym $mod+F12 exec --no-startup-id fish -c touchpadToggle

# Start common applications
bindsym $mod+Return exec --no-startup-id urxvt -e fish
bindsym $mod+t exec --no-startup-id urxvt -e fish
bindsym $mod+F3 exec --no-startup-id i3-sensible-terminal -e ranger

# Scratchpad
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show
bindsym $mod+Shift+equal move scratchpad
bindsym $mod+equal scratchpad show
# kill
bindsym $mod+q kill
bindsym $mod+shift+z exec xkill

# Rofi run and window switch dialogues
bindsym $mod+Tab exec --no-startup-id rofi -show run -no-levenshtein-sort

# screenshot
bindsym --release Print exec scrot -d 3 ~/Pictures/screenshot-$(date +%Y-%m-%d_%H-%M-%S).png
bindsym --release Shift+Print exec scrot -d 3 -s ~/Pictures/screenshot-$(date +%Y-%m-%d_%H-%M-%S).png

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Split in horizontal orientation
bindsym $mod+z split h

# Split in vertical orientation
bindsym $mod+x split v

# Enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# Change focus
bindsym $mod+a focus left
bindsym $mod+s focus down
bindsym $mod+w focus up
bindsym $mod+d focus right
bindsym $mod+u [urgent=latest] focus

# Move focused window
bindsym $mod+Shift+a move left
bindsym $mod+Shift+s move down
bindsym $mod+Shift+w move up
bindsym $mod+Shift+d move right

# Move workspaces between windows
bindcode $mod+112 move workspace to output left
bindcode $mod+117 move workspace to output right

# Change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
bindsym $mod+c layout tabbed
bindsym $mod+e layout toggle split

# Toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# Change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+m focus parent

# focus the child container
bindsym $mod+n focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# Alternatively, you can use the cursor keys:
bindsym $mod+Left workspace next_on_output
bindsym $mod+Right workspace prev_on_output
bindsym $mod+Down focus output left
bindsym $mod+Up focus output right

# move commands
bindsym $mod+Shift+Left move container to workspace next_on_output
bindsym $mod+Shift+Right move container to workspace prev_on_output
bindsym $mod+Shift+Down move container to output left
bindsym $mod+Shift+Up move container to output right

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10



# Reload the configuration file
bindsym $mod+Shift+c reload

# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# Exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# Resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (global)

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

mode "$mode_gaps_inner" {
        bindsym plus  gaps inner all plus 3
        bindsym minus gaps inner all minus 3
        bindsym 0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer all plus 3
        bindsym minus gaps outer all minus 3
        bindsym 0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}

set $mode_options Options: (k) keyboard layout, (r) resize, (g) gaps
bindsym $mod+o mode "$mode_options"

mode "$mode_options" {
        bindsym k     mode "$mode_keyboard_layout"
        bindsym r     mode "resize"
        bindsym g     mode "$mode_gaps"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
set $mode_keyboard_layout Keyboard layout: (u) us, (d) de, (c) colemak
mode "$mode_keyboard_layout" {
        bindsym u     exec ~/bin/xkb.fish us; mode "default"
        bindsym d     exec ~/bin/xkb.fish de; mode "default"
        bindsym c     exec ~/bin/xkb.fish colemak; mode "default"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bar {
        status_command python ~/.config/i3/i3status.py 0.5
        font pango: DejaVu Sans 13
        position bottom
        mode dock
        workspace_buttons yes
        strip_workspace_numbers yes
        binding_mode_indicator yes

        colors {
            background         #2d2d2d
            statusline         #00ffff
            separator          #33D04E
            ################## border  bground text
            focused_workspace  #d3d0c8 #d3d0c8 #2d2d2d
#            focused_workspace  #6699cc #6699cc #111111
            active_workspace   #2d2d2d #2d2d2d #d3d0c8
            inactive_workspace #2d2d2d #2d2d2d #d3d0c8
            urgent_workspace   #a52a2a #a52a2a #e0e0e0
        }
}

assign [class="Firefox"] 1
assign [class="Thunderbird"] 3

for_window [class="Wicd"] floating enable
for_window [class="Xfce4-notifyd"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
workspace_auto_back_and_forth yes
focus_follows_mouse no
smart_borders on
mouse_warping none
force_display_urgency_hint 1000 ms
new_window pixel 5
new_float none
gaps inner 10

# Font for window titles.
font pango: DejaVu Sans 13
# class                 border  backgr. text    indicator
client.focused          #d27b53 #d27b53 #e0e0e0 #d27b53
client.focused_inactive #333333 #333333 #e0e0e0 #e0e0e0
client.unfocused        #333333 #333333 #e0e0e0 #e0e0e0
client.urgent           #333333 #333333 #e0e0e0 #e0e0e0

# class                 border  backgr. text    indicator
# client.focused          #2aa198 #073642 #93a1a1 #073642
# client.focused_inactive #2aa198 #073642 #586e75 #073642
# client.unfocused        #002b36 #002b36 #586e75 #002b36
# client.urgent           #002b36 #002b36 #dc322f #002b36

# Original
# class                  border  backgr. text    indicator
#client.focused          #4c7899 #2855aa #ffffff #2e9ef4
#client.focused_inactive #333333 #5f676a #ffffff #484e50
#client.unfocused        #333333 #222222 #888888 #292d2e
#client.urgent           #2f343a #900000 #ffffff #900000
#client.placeholder      #000000 #0c0c0c #ffffff #000000

# autostart
exec --no-startup-id redshift-gtk
exec --no-startup-id ~/.fehbg
exec --no-startup-id syndaemon -t -k -i 2 -d
exec --no-startup-id ~/.config/i3/screenAutoLock.sh
exec --no-startup-id ~/bin/padXORmouse.sh
exec --no-startup-id ~/bin/xkb.fish
exec --no-startup-id nm-applet
exec --no-startup-id dunst
exec --no-startup-id dropbox start
exec --no-startup-id clipit
exec --no-startup-id xrdb ~/.Xresources
exec --no-startup-id skype
exec --no-startup-id firefox
exec --no-startup-id thunderbird

