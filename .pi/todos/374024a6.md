---
id: 374024a6
title: "LSP: `ensureOpen` is async but does nothing async"
tags: [minor, lsp]
status: closed
created_at: "2026-02-18T19:13:37.901Z"
---

**Minor** — `ensureOpen` in `client.ts` is declared `async` but only calls the synchronous `notify()` and `Set.add()`. The `async` keyword is unnecessary (though harmless).

**Fix:** Remove `async` from the method signature, or leave as-is for future-proofing if async work might be added later.
