---
id: 9d8d9a8a
title: "[P1] LSP config: .zed/settings.json is JSONC, JSON.parse rejects it"
tags: [lsp, p1, bug]
status: open
created_at: "2026-02-15T21:59:47.181Z"
---

**File:** `agents/extensions/lsp/config.ts:70`

Zed's settings files routinely contain `//` comments and trailing commas. `JSON.parse` throws on those, so the Zed fallback path is effectively dead for most real configs. The error is caught and logged to stderr (which the user never sees), then `loadConfig` returns `null`.

**Fix:** Use a JSONC-tolerant parser — either strip comments before parsing or use a library.
